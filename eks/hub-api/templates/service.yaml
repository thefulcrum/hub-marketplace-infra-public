apiVersion: v1
kind: Service
metadata:
  name: {{ include "hub-api.fullname" . }}
  labels:
    {{- include "hub-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    {{- include "hub-api.selectorLabels" . | nindent 4 }}
